{
  "title": "GitHub Actions for plugin",
  "description": "GitHub Actions for Swift-setup plugin workflow",
  "templates": [
    {
      "name": "sample.yaml",
      "description": "Sample GitHub Actions for Swift-setup plugin workflow",
      "outputFilePath": ".github/workflows/test.yaml"
    },
    {
      "name": "package.py",
      "description": "Helper script to package the plugin",
      "outputFilePath": "package.py"
    }
  ]
}
